import { MassFormatPipe } from './mass-format.pipe';

describe('MassFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MassFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
